import { Node } from 'typescript';
import { TSQueryNode } from './tsquery-types';
export declare function query<T extends Node = Node>(ast: string | Node | TSQueryNode<T>, selector: string): Array<TSQueryNode<T>>;
