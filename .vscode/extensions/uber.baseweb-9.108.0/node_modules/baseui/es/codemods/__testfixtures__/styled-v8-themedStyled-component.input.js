import { styled } from 'baseui';

function A(props) {
  return React.createElement("div", {
    className: props.className
  }, "abcd");
}

const B = styled(A, props => {
  return {
    color: props.$theme.color
  };
});
B.displayName = "B";